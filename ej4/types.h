#ifndef TYPES_H
#define TYPES_H

/* Definición de tipos estándar para toda la práctica */
typedef enum { 
    ERROR = 0, 
    OK = 1 
} Status;

typedef enum { 
    FALSE = 0, 
    TRUE = 1 
} Bool; 

#endif